import React from "react";
import Layout from "../components/Layout";

const HomePage = () => (
  <Layout pageTitle="Home Page">
    <p>This is from the Gatsby tutorial</p>
  </Layout>
);

export const Head = () => <title>Home Page</title>

export default HomePage;
